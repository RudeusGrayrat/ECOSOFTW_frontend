import Approve from "../../../../components/Principal/Permissions/Approve";

const ApproveCotizacion = ({ selected, setShowApprove }) => {
    return (
        <Approve setShowApprove={setShowApprove} >

        </Approve>
    )
}

export default ApproveCotizacion;